{
  "name": "rozpisnyi-poker",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run build -w packages/shared && concurrently \"npm run dev -w apps/server\" \"npm run dev -w apps/client\"",
    "build": "npm run build -w packages/shared && npm run build -w apps/server && npm run build -w apps/client",
    "typecheck": "npm run build -w packages/shared && npm run typecheck --workspaces --if-present",
    "test": "npm run test -w packages/shared"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
